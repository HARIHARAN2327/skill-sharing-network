# === Local Development .env (example) ===
# Adjust values as needed for your local PostgreSQL.

SPRING_PROFILES_ACTIVE=dev

DB_URL=jdbc:postgresql://localhost:5432/app_db
DB_USERNAME=postgres
DB_PASSWORD=postgres
JPA_DDL_AUTO=update
